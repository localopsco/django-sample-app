apiVersion: s3.aws.upbound.io/v1beta1
kind: Bucket
metadata:
  name: localops-sample-bucket-{{ .Release.Name }}-1
spec:
  forProvider:
    region: ap-south-1
  providerConfigRef:
    name: default

---

apiVersion: s3.aws.upbound.io/v1beta1
kind: Bucket
metadata:
  name: localops-sample-bucket-{{ .Release.Name }}-2
spec:
  forProvider:
    region: ap-south-1
  providerConfigRef:
    name: default
